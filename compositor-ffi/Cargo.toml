[package]
name = "compositor-ffi"
version = "2.0.0"
edition = "2021"

[dependencies]
compositor = { path = "../compositor" }
compositor-skia = { path = "../compositor-skia" }
reference-box = "1.0"
string-box = "1.1"
array-box = "1.0"
value-box = { version = "2", features = [ "phlow" ] }